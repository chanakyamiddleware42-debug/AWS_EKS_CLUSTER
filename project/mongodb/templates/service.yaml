apiVersion: v1 
kind: Service
metadata:
  name: mongodb
  labels:
    project: roboshop
    application: mongodb
spec:
  type: {{ .Values.service.type }}
  selector:
    project: roboshop
    application: mongodb
  ports:
  - port: 27017
    targetPort: 27107